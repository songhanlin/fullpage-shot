{
  "name": "fullpage-shot",
  "version": "1.0.0",
  "private": true,
  "license": "MIT",
  "description": "整页滚动截图、自由裁剪、导出 Word / PDF 的 Chrome 扩展",
  "type": "module",
  "scripts": {
    "icons": "node scripts/gen-icons.mjs",
    "build": "node scripts/gen-icons.mjs && node build.mjs",
    "pack": "npm run build && rm -f fullpage-shot.zip && (cd dist && zip -qr ../fullpage-shot.zip .) && echo \"打包完成 → fullpage-shot.zip（用于上传 Chrome 应用商店）\""
  },
  "dependencies": {
    "docx": "^9.6.1",
    "jspdf": "^4.2.1"
  },
  "devDependencies": {
    "esbuild": "^0.28.0"
  }
}
