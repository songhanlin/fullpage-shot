{
  "name": "fullpage-shot",
  "version": "1.0.0",
  "private": true,
  "description": "整页滚动截图、自由裁剪、导出 Word / PDF 的 Chrome 扩展",
  "type": "module",
  "scripts": {
    "icons": "node scripts/gen-icons.mjs",
    "build": "node scripts/gen-icons.mjs && node build.mjs"
  },
  "dependencies": {
    "docx": "^9.6.1",
    "jspdf": "^4.2.1"
  },
  "devDependencies": {
    "esbuild": "^0.28.0"
  }
}
